const inputs = `
<input type="{{type}}" class="field-input {{inputClasses}} " name="{{name}}" value="{{value}}" placeholder="{{placeholder}}" {{#if disabled}} disabled {{/if}}>
`;
export default inputs;
