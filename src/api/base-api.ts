export class BaseAPI {
  create(props: any) {
    throw new Error(`Not implemented ${props}`);
  }

  request(props: any) {
    throw new Error(`Not implemented ${props}`);
  }

  update(props: any) {
    throw new Error(`Not implemented ${props}`);
  }

  delete(props: any) {
    throw new Error(`Not implemented ${props}`);
  }
}
